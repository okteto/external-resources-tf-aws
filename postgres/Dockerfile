# This is a placeholder Dockerfile for the postgres build context
# The actual PostgreSQL deployment uses the official postgres image
FROM postgres:15-alpine

# This Dockerfile is required for the build context but won't be used
# since we deploy PostgreSQL using the Helm chart with the official image